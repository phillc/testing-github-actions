name: Publish Docker Release
on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Test
      run: echo "asdf,${{github.ref}},${{github.event_name}}${{github.action}}${{github.event_path}}${{github.event.id}}${{github.event.tag_name}}"

